#include <stdio.h>
void push(int *arr[], int top, int n){

    printf("Enter an number to push:-");
    scanf("%d\n",&n);
}
int main(){
        int size=("Enter the size of stack:");
    int arr[size];
}
